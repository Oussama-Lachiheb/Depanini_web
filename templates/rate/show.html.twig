{% extends 'back.html.twig' %}

{% block title %}Rate{% endblock %}

{% block body %}
    <h1>Rate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rate.id }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ rate.rate }}</td>
            </tr>
            <tr>
                <th>Client</th>
                <td>{{ rate.idClient.nom }}</td>
            </tr>
            <tr>
                <th>Prestataire</th>
                <td>{{ rate.idPrestataire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rate_index') }}">back to list</a>

    <a href="{{ path('app_rate_edit', {'id': rate.id}) }}">edit</a>

    {{ include('rate/_delete_form.html.twig') }}
{% endblock %}
