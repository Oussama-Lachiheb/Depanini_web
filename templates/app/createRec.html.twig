{% extends 'base.html.twig' %}

{% block title %}Ajouter une reclamation !{% endblock %}

{% block body %}
<div class="container px-4 px-lg-5 h-100">
    <div class="row gx-4 gx-lg-5 h-100 align-items-center justify-content-center text-center">
        <div class="col-lg-8 align-self-end">
            <h1 class="text-white font-weight-bold">Ajouter une reclamation !</h1>
                {{ form_start(formReclamation) }}
                {{ form_row(formReclamation.typeReclamation) }}
                {{ form_row(formReclamation.descriptionReclamation) }}
        </div>
        <button href="{{ path('app_list_type' )}}" type="submit" class="btn btn-primary btn-l">Ajouter Reclamation</button>
        {{ form_end(formReclamation) }}
    </div>
</div>

{% endblock %}
