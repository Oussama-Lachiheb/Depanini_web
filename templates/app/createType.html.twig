{% extends 'base.html.twig' %}
{% block title %}Ajouter un type de reclamation !{% endblock %}

{% block body %}
    <div class="container px-4 px-lg-5 h-100">
        <div class="row gx-4 gx-lg-5 h-100 align-items-center justify-content-center text-center">
        <div class="col-lg-8 align-self-end">
            <h1 class="text-white font-weight-bold">Ajouter un type de reclamation !</h1>
            {{ form_start(formTypeReclamation) }}
            {{ form_row(formTypeReclamation.descriptionType) }}
        </div>
            <button type="submit" class="btn btn-primary btn-l">Enregistrer</button>
            {{ form_end(formTypeReclamation) }}
        </div>
    </div>
{% endblock %}