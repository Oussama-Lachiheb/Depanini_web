{% extends 'baseback.html.twig' %}



{% block body %}
    <h1>Typepromo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdType</th>
                <td>{{ typepromo.idType }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typepromo.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typepromo_index') }}">back to list</a>

    <a href="{{ path('app_typepromo_edit', {'idType': typepromo.idType}) }}">edit</a>

    {{ include('typepromo/_delete_form.html.twig') }}
{% endblock %}

